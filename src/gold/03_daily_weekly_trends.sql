-- Purpose : Build gold_daily_weekly_trends aggregation table.
--           Summarises daily and weekly revenue trends over time,
--           sourced from Silver PASSED rows only.
-- Inputs  : workspace.ecommerce_medallion.silver_orders (quality_check_result = 'PASSED')
-- Outputs : workspace.ecommerce_medallion.gold_daily_weekly_trends (Delta table)
-- Columns : order_date, week_start_date, daily_revenue, weekly_revenue,
--           daily_order_count, weekly_order_count
-- Phase   : Phase 4 — Gold Layer

-- ── Implementation will be added in Phase 4 ─────────────────────────────────
